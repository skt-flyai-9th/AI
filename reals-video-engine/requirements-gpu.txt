# ⚠ 대상: RTX 4090 Laptop GPU — VRAM 16GB (데스크톱 4090 24GB 아님)
# ⚠ torch는 이 파일로 설치하지 말 것 — README의 전용 명령으로 먼저 설치:
#     pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
# ⚠ CPU용 onnxruntime과 충돌: 먼저 `pip uninstall -y onnxruntime`

onnxruntime-gpu==1.26.0
rapidocr-onnxruntime>=1.3
ultralytics>=8.3
transformers>=4.57
accelerate>=1.0
qwen-vl-utils>=0.0.11
psutil>=5.9
# 선택: TwelveLabs Pegasus 클라우드 폴백
# twelvelabs>=0.4
# 검증된 SAM 3.1 소스 커밋
sam3 @ git+https://github.com/facebookresearch/sam3.git@8f0b7f4d4e7eda2ed606ebde6702c93359ad01da
